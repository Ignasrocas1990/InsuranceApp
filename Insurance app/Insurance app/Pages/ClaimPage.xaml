<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Insurance_app.ViewModels;assembly=Insurance app"
             x:DataType="viewModels:ClaimViewModel"
             x:Class="Insurance_app.Pages.ClaimPage">
    
    
    <Grid VerticalOptions="CenterAndExpand" RowSpacing="20" ColumnSpacing="5" Padding="40">
              <Grid.RowDefinitions>
            
                     <RowDefinition Height="Auto"/>
                     <RowDefinition Height="Auto"/>
                     <RowDefinition Height="Auto"/>
                     <RowDefinition Height="Auto"/>
                     <RowDefinition Height="Auto"/>
                     <RowDefinition Height="*"/>

              </Grid.RowDefinitions>
        
              <Grid.ColumnDefinitions>
                     <ColumnDefinition Width="5*"/>
                     <ColumnDefinition Width="5*"/>
              </Grid.ColumnDefinitions>
                
            <Label Text="{Binding DateDisplay}" FontSize="20" VerticalTextAlignment="Center" HorizontalTextAlignment="End" TextColor="Black" BackgroundColor="White" 
                   Grid.Column="0" Grid.Row="0" Grid.ColumnSpan="2" />
            
            <Label Text="Hospital P. Code:" FontSize="20" VerticalTextAlignment="Center" HorizontalTextAlignment="End" TextColor="Black" BackgroundColor="White" Grid.Column="0" Grid.Row="1" />
            <Entry  Keyboard="Email" IsEnabled="{Binding FieldEnabledDisplay}"
                    Text="{Binding  HospitalPostCodeDisplay}" 
                    Placeholder="R93 V1N6" PlaceholderColor="DimGray" FontSize="20" TextColor="Black" BackgroundColor="White"
                    Grid.Column="1" Grid.Row="1"/>
                
            <Label Text="Patient Nr :" FontSize="20" VerticalTextAlignment="Center" HorizontalTextAlignment="End" TextColor="Black" BackgroundColor="White" Grid.Column="0" Grid.Row="2" />
            <Entry  Text="{Binding PatientNrDisplay}" IsEnabled="{Binding FieldEnabledDisplay}"
                    Placeholder="123S3DG23S" PlaceholderColor="DimGray" FontSize="20" TextColor="Black" BackgroundColor="White"
                    Grid.Column="1" Grid.Row="2"/>
              
            <Label Text="{Binding StatusDisplay}" IsEnabled="False" 
                   Grid.Column="0" Grid.Row="3"  FontSize="20" VerticalTextAlignment="Center" HorizontalTextAlignment="Center" TextColor="Black" BackgroundColor="White" />
            
            <Button Text="AllClaims" Command="{Binding ViewPreviousClaimsCommand}" IsEnabled="{Binding PreviousButtonEnabled}"
                    Grid.Column="0" Grid.Row="4" TextColor="Black"  HorizontalOptions="Center" BackgroundColor="Indigo" />
            <Button Text="Create"  Command="{Binding  CreateClaimCommand}"  FontSize="20" TextColor="Black" BackgroundColor="Green"
                    Grid.Column="1" Grid.Row="4" IsEnabled="{Binding FieldEnabledDisplay}" />
              
            
            <ActivityIndicator IsRunning="{Binding CircularWaitDisplay}" 
                    HorizontalOptions="Center" VerticalOptions="Start" Color="Gray" Grid.Column="0" Grid.Row="5" Grid.ColumnSpan="2" />
    </Grid>           
    
</ContentPage>