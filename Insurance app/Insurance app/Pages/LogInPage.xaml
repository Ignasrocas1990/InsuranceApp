<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels1="clr-namespace:Insurance_app.ViewModels"
             x:DataType="viewModels1:LogInViewModel"
             Shell.PresentationMode="Modal"
             Shell.FlyoutBehavior="Disabled"
             Shell.NavBarIsVisible="False"
             x:Class="Insurance_app.Pages.LogInPage">
    <ContentPage.BindingContext>
        <viewModels1:LogInViewModel/>
    </ContentPage.BindingContext>
    
    <StackLayout Padding="40" VerticalOptions="Center" HorizontalOptions="FillAndExpand">
        <Entry Text="{Binding EmailDisplay }"  Placeholder="email@gmail.com" TextColor="Black" PlaceholderColor="Gray" IsTextPredictionEnabled="True" Keyboard="Plain" />
                <Entry Text= "{Binding PasswordDisplay}" Placeholder="secret" TextColor="Black" PlaceholderColor="Gray"  IsPassword="True" Keyboard="Plain" />
                <Button Text="Log In" Command="{Binding LogInCommand}" />
                <Button Text="Get a Quote" Command="{Binding QuoteCommand }" />
    </StackLayout>
        
</ContentPage>