<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModels="clr-namespace:Insurance_app.ViewModels;assembly=Insurance app"
             x:DataType="viewModels:QuoteViewModel"
             Shell.FlyoutBehavior="Disabled"
             Shell.NavBarIsVisible="False"
             x:Class="Insurance_app.Pages.QuotePage">
    <ContentPage.Content>
        <StackLayout>
            <ActivityIndicator
                IsRunning="{Binding CircularWaitDisplay}" HorizontalOptions="Center" VerticalOptions="Start" Color="Blue" />
            
            <Button Text= "Get Quote " Command="{Binding GetQuotCommand}"></Button>
        </StackLayout>
        
    </ContentPage.Content>
</ContentPage>